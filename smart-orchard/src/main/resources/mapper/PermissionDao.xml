<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hz.smartorchard.dao.PermissionDao">

<select id="findPermissionPathByUserId" resultType="java.lang.String">

    select
        c.permissionValue
    from soms_user_role_relation a
    left join
    soms_role_permission_relation b
    on
    a.roleId = b.roleId
    left join
    soms_permission c
    on
    b.permissionId = c.permissionId
    where a.userId = #{userId}
    </select>
</mapper>
